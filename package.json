{
  "name": "transactions-sample-app",
  "description": "This is a sample of an Assistant app using Transactions",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "Google Inc.",
  "engines": {
    "node": "~6.0"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "start": "functions deploy transactions --trigger-http",
    "deploy": "gcloud beta functions deploy transactions --trigger-http --stage-bucket <storage_bucket_name>"
  },
  "dependencies": {
    "actions-on-google": "^1.0.0",
    "googleapis": "^19.0.0",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  }
}
